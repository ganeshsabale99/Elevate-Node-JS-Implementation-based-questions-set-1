// 5. File Watcher Create watchDirectory(dirPath, callback) that monitors a directory for file changes 
// and executes a callback with change details.

const fs = require("fs");
const path = require("path");
