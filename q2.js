// 2. File Backup Creator Create backupFile(filePath) that creates a backup copy of a file with timestamp in the filename
//  (e.g., document_2024-06-24_10-30.txt).

const fs = require("fs");

function backupFile(filePath) {
   
}

backupFile(filePath);

