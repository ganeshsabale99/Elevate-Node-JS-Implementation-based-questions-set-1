// 1. Directory Size Calculator Implement calculateDirSize(dirPath) that recursively calculates 
// the total size of all files in a directory and its subdirectories.


const fs = require("fs");

function calculateDirSize(dirPath) {
   
}

const size = calculateDirSize(dirPath);
console.log(`Total size of ${dirPath} is ${size} bytes`);


